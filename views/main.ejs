<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Blog</title>
  <link rel="stylesheet" href="/styles/main.css" />
</head>

<body>
  <div class="container">
    <h1>📝 My Blog</h1>
    <a id="newPostBtn" href="/new">+ New Post</a>

    <ul id="postsList">
      <% posts.forEach(post => { %>
        <li class="post-card">
          <h2><%= post.title %></h2>
          <small class="post-date">🗓️ <%= new Date(post.date).toLocaleDateString() %></small>
          <p><%= post.content %></p>
          <small class="post-author">✍️ By <%= post.author %></small>
          <div class="action-buttons">
            <a class="edit" href="/edit/<%= post.id %>">Edit</a>
            <a class="delete" href="/api/posts/delete/<%= post.id %>">Delete</a>
          </div>
        </li>
      <% }); %>
    </ul>
  </div>
</body>

</html>
